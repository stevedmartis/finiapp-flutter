import 'package:flutter/material.dart';

const primaryColor = Color(0xFF2697FF);
const backgroundDark = Color(0xFF212332);
const cardDark = Color(0xFF1F222D);
const bgColor = Color(0xFF212332);

const logoCOLOR1 = Color(
    0xFFA668E9); // Asegúrate de que el canal alfa está en FF para máxima opacidad
const logoCOLOR2 = Color(
    0xFF410096); // Asegúrate de que el canal alfa está en FF para máxima opacidad

const defaultPadding = 16.0;

const lightBackground = Color(0xFFF5F5F5); // Light Mode Background
const lightTitleColor = Color(0xFF333333); // Light Mode Titles
const lightSubtitleColor = Color(0xFF666666); // Light Mode Subtitles
const darkTitleColor = Color(0xFFFFFFFF); // Dark Mode Titles
const darkSubtitleColor = Color(0xFFBBBBBB); // Dark Mode Subtitles
const lightIconColor = Color(0xFF410096); // Light Mode Icons
const darkIconColor = Color(0xFFA668E9); // Dark Mode Icons
const lightCardBackground = Color(0xFFF0F0F0); // Light Mode Card Background
const darkCardBackground = Color(0xFF2A2D3E); // Dark Mode Card Background
const lightModeIcon = Color(0xFFFFEB3B);
